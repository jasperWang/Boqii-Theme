<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:background="@color/color_white"
    android:orientation="vertical" >

    <ListView 
        android:id="@+id/listview"
        android:layout_width="fill_parent"
        android:layout_height="fill_parent"
        android:layout_weight="1"
        android:scrollbars="none"
        android:fadingEdge="none"
        android:dividerHeight="1px"
        android:divider="@drawable/ic_xuxian"
        android:listSelector="@android:color/transparent"
        android:cacheColorHint="@android:color/transparent"
        ></ListView>

    <LinearLayout
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:gravity="center_vertical"
        android:orientation="horizontal" >

        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content" >
             <TextView
                android:id="@+id/collect"
                android:layout_width="100dip"
                android:layout_height="fill_parent"
                android:paddingBottom="5dip"
                android:gravity="bottom|center_horizontal"
                android:paddingTop="12dip"
                android:textColor="@color/color_black_text"
                android:drawableTop="@drawable/ic_good_detail_collect"
                android:textSize="12dip" 
                android:background="@drawable/ic_goode_detail_background"/>

            <TextView
                android:id="@+id/praise"
                android:layout_width="100dip"
                android:layout_height="fill_parent"
                android:background="@drawable/ic_goode_detail_background"
                android:drawableTop="@drawable/ic_good_detail_zan"
                android:paddingBottom="5dip"
                android:gravity="bottom|center_horizontal"
                android:paddingTop="12dip"
                android:textColor="@color/color_black_text"
                android:textSize="12dip" />
        </LinearLayout>

        <TextView
            android:id="@+id/buy"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:background="@drawable/ic_buy"
            android:gravity="bottom|center_horizontal"
            android:paddingBottom="12dip"
            android:text="加入购物车"
            android:textColor="@color/color_white"
            android:textSize="18dip" />
    </LinearLayout>

</LinearLayout>